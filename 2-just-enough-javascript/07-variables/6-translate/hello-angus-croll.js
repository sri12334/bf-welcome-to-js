// #todo

'use strict';

// first <- 'angus'
const first = 'image';

// last <- 'croll'
const last = 'croll';

// greeting <- 'hello ' + first + ' ' + last
let greeting = 'hello ' + first + ' ' + last;

// greeting <- greeting + '!'
greeting = greeting + '!';

console.log(greeting);
